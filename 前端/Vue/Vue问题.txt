1.如何定义一个Vue和Vue组件都能访问到的属性？VueComponent.prototype._proto__ === Vue.prototype
1.数据单向绑定和双向绑定如何实现？ v-bind/:value v-model


2.如果要监测一个值的变化，我们可以使用Vue的哪个属性？ 监视属性：watch
2.Vue如何绑定一个事件？v-on/@click

3.如果我们需要控制HTML中的某个元素是否展示，可以使用Vue的哪个指令？v-show/v-if
3.如果我们需要遍历列表中的数据，我们可以用Vue的那个指令。

4.Vue中常用的生命周期钩子。mounted：真实的DOM元素挂载到页面上时；beforeDestroy：关闭定时器、取消订阅消息、解绑自定义事件
4.如果一个值需要根据其他的值生成，我们可以使用Vue的哪个属性？ 计算属性：computed

5.如何实现两个组件之间传递值？props、全局事件总线、消息发布与订阅
5.Vue单文件组件时，组件中的样式如何实现全局使用/当前组件使用？scoped

6.当组件中的一些结构相同，但部分结构不同时，如何动态的将结构传进来？slot插槽
6.Vue路由时，如果需要保留浏览器历史记录，应该使用push还是replace方法进行路由跳转？ push

7.Vue进行路由时，怎么实现不满足某个条件就不路由到对应页面？路由守卫。